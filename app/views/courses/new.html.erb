<h1>New Course</h1>

<%= render "layouts/errors", :object => @course %>

<%= form_for @course do |course| %>
  <%= course.label :name %>
  <%= course.text_field :name %>
  <%= course.submit %>
<% end %>
