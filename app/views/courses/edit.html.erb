<h1>Edit Courses</h1>

<% if @course.errors.any? %>
  <h3>Please fix these errors:</h3>
  <ul>
    <% @course.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= render "form" %>
